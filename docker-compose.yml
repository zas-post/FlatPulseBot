version: '3.8'

services:
  bot:
    build: .
    container_name: flat_pulse_bot
    restart: always
    volumes:
      - ./flat_pulse.db:/app/flat_pulse.db
      - ./errors.log:/app/errors.log
    env_file:
      - .env
